import { BaseTemplate } from '@templates/base'
import { MessageRequired } from '@widgets/message-required'

export const MessageRequiredPage = () => {
  return (
    <BaseTemplate>
      <MessageRequired />
    </BaseTemplate>
  )
}
